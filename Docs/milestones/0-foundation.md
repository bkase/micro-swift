# Milestone 0 — Foundation

This document tracks the local-first, deterministic foundation milestone.

- One root package with executable CLI target.
- Point-Free-shaped command architecture (`AsyncParsableCommand`).
- Dependency-injected shell capabilities for process, files, environment, etc.
- Canonical `./dev verify` pipeline plus pre-commit enforcement.
