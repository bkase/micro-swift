#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "usage: ./mlx-run <binary> [args...]" >&2
  exit 1
fi

BINARY="$1"
shift
if [[ ! -x "$BINARY" ]]; then
  echo "not executable: $BINARY" >&2
  exit 1
fi

"$BINARY" "$@"
